{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "np serve",
            "type": "debugpy",
            "request": "launch",
            "module": "np_service",
            "args": ["serve"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
